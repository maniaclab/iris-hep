apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{owner}}-cephfs-pvc
  namespace: {{namespace}}
  labels:
    owner: {{owner}}
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  storageClassName: rook-cephfs